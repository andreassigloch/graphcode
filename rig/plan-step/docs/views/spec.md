<!-- GENERATED by @sigloch/graphcode exportMarkdown — DO NOT HAND-EDIT.
     Source of truth: docs/graph/graphcode.graph.json (the live graph).
     Re-render on model change: node scripts/export-graph.mjs -->

# graphcode — Modell-Spezifikation

> GENERATED from `docs/graph/graphcode.graph.json` (SSOT). Alle Elemente nach Typ, sortiert nach uid. Deterministisch generiert.

Elemente: 105 · Traces: 213


## ACTOR

| uid | name | status | description |
|---|---|---|---|
| `ACTOR-admin` | Administrator |  | Nutzer mit Governance-Rechten zur Batch-Genehmigung und Regelabsetzung |
| `ACTOR-governance-reviewer` | Governance Reviewer |  | System architect reviewing authored graph changes for compliance |
| `ACTOR-graph-user` | Graph User |  | End-user discovering and filtering graph elements |
| `ACTOR-system` | Harness-System |  | Automatisiertes System, verwaltet Apply-Gate und Exporte |
| `ACTOR-user` | Nutzer |  | Dateneigner, meldet sich an und erstellt Graphelemente |

## CR

| uid | name | status | description |
|---|---|---|---|
| `CR-PS-001` | Login & Session | open | Realisiert FUNC-authenticate-user, FUNC-create-session → REQ-user-auth-email, REQ-session-token-created. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-002` | Validate Mutation (unit) | open | Realisiert FUNC-validate-mutation → REQ-author-gate-validation, REQ-author-input-validation. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-003` | Validate Mutations (batch) | open | Realisiert FUNC-validate-mutations → REQ-apply-gate-verdict-deterministic, REQ-batch-atomicity-measurable. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-004` | Approve Batch | open | Realisiert FUNC-approve-batch → REQ-batch-atomicity-measurable. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-005` | Persist Batch | open | Realisiert FUNC-persist-batch → REQ-governance-enforcement. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-006` | Edit Element | open | Realisiert FUNC-edit-element → REQ-author-integrity. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-007` | Serialize Graph | open | Realisiert FUNC-serialize-graph → REQ-export-snapshot-bitwise-identical, REQ-export-versioning. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-008` | Search Index | open | Realisiert FUNC-search-index → REQ-search-indexing, REQ-search-result-precision-recall. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-009` | Search Filter | open | Realisiert FUNC-search-filter → REQ-search-filtering, REQ-user-discover-search-interface. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-010` | Broadcast WebSocket | open | Realisiert FUNC-broadcast-websocket → REQ-websocket-live. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |
| `CR-PS-011` | Subscribe Updates | open | Realisiert FUNC-subscribe-updates → REQ-updates-visible-ui. Context = graph_context-Slice dieser Knoten (se-plan, CR-GC-283). |

## FCHAIN

| uid | name | status | description |
|---|---|---|---|
| `FCHAIN-apply-gate-validation` | Apply-Gate Validation Chain |  | Validation flow: OCC check → tier verdict → mutations → graph update |
| `FCHAIN-authoring-scenario` | Authoring-Szenario |  | Benutzer erstellt/editiert Graphelemente mit Governance-Validierung |
| `FCHAIN-batch-approval-flow` | Batch-Genehmigungsablauf |  | Szenario: Admin prüft Violations, genehmigt oder lehnt ab |
| `FCHAIN-concurrent-authoring` | Concurrent Authoring Flow |  | Node creation → OCC validation → gate verdict → mutation apply |
| `FCHAIN-export-lifecycle` | Export Lifecycle |  | Graph serialization → persistence → client delivery |
| `FCHAIN-export-scenario` | Export-Szenario |  | Szenario: Nutzer triggert Export, System serialisiert Graphen |
| `FCHAIN-export-standard-format` | Export Standard Format Chain |  | Export flow: select elements → serialize version → write file |
| `FCHAIN-filter-by-type-and-search` | Filter By Type And Search Chain |  | Discovery flow: apply type filter → apply search query → return results |
| `FCHAIN-governance-enforcement-flow` | Governance-Durchsetzungsablauf |  | Szenario: System wendet Regeln auf Batch an, persistiert |
| `FCHAIN-live-update-scenario` | Live-Update-Szenario |  | Benutzer empfängt WebSocket-Updates bei Änderungen anderer Nutzer |
| `FCHAIN-live-update-stream` | Live Update Stream |  | Streaming functions for real-time graph updates across sessions |
| `FCHAIN-login-auth-session` | Login–Authentifizierung–Session |  | Szenario: Nutzer loggt sich ein, wird authentifiziert, erhält Session-Token zur Graphbearbeitung |
| `FCHAIN-login-scenario` | Login-Szenario |  | Benutzer authentifiziert sich in der Web-App |
| `FCHAIN-monitor-ws-broadcast` | Monitor–WebSocket–Broadcast |  | Szenario: Nutzer verbindet WebSocket, erhält Live-Updates von Mutations-Events anderer Aktoren |
| `FCHAIN-multiuser-auth-flow` | Multiuser Authentication Flow |  | Sequential functions for user authentication in multiuser context |
| `FCHAIN-search-scenario` | Suchszenario |  | Benutzer findet und filtert Elemente im Live-Graphen |
| `FCHAIN-update-propagation` | Update Propagation |  | Mutation event → broadcast → client render |
| `FCHAIN-view-apply-gate-result` | View–Apply-Gate–Result |  | Szenario: Nutzer sieht UI-Update mit erfolgreicher oder blockierter Mutation durchs Apply-Gate |

## FLOW

| uid | name | status | description |
|---|---|---|---|
| `FLOW-approved-batch` | Approveter Batch |  | Freigegebener Batch → atomare Persistierung (All-or-Nothing). |
| `FLOW-auth-result` | Auth-Ergebnis |  | Geprüfte Credentials → Session-Erzeugung (Erfolg/Fehlschlag + User-Identität). |
| `FLOW-edit-commands` | Edit-Kommandos |  | Vom Editor erzeugte Mutations-Kommandos (add/update/delete) zur Einzelvalidierung. |
| `FLOW-gate-verdict` | Gate-Verdict |  | Batch-Verdict (tier/violations) → Approval-Entscheidung. |
| `FLOW-graph-delta` | Graph-Delta |  | Persistierte Änderungsmenge — Quelle für Export, Suchindex und Live-Broadcast. |
| `FLOW-index-entries` | Index-Einträge |  | Aktualisierte Suchindex-Einträge → Filter/Query. |
| `FLOW-session-token` | Session-Token |  | Gültiges Session-Token — Voraussetzung für jede Authoring-Aktion. |
| `FLOW-update-event` | Update-Event |  | Versioniertes Broadcast-Event → Subscriber-UI. |
| `FLOW-validated-commands` | Validierte Kommandos |  | Einzeln geprüfte Kommandos → Batch-Validierung gegen V3_RULES. |

## FUNC

| uid | name | status | description |
|---|---|---|---|
| `FUNC-approve-batch` | Approve Batch |  | Approves or rejects mutation batch through Apply-Gate |
| `FUNC-authenticate-user` | Authenticate User |  | Validates user credentials against identity provider |
| `FUNC-broadcast-websocket` | Broadcast WebSocket |  | Sends real-time updates to connected clients |
| `FUNC-create-session` | Create Session |  | Establishes authenticated session for graph operations |
| `FUNC-edit-element` | Edit Element |  | Creates or updates graph element with validation |
| `FUNC-persist-batch` | Persist Batch |  | Commits validated mutations to graph store |
| `FUNC-search-filter` | Elemente nach Typ filtern |  | Filtert Graphelemente nach Typ, UID, Name |
| `FUNC-search-index` | Suchindex abfragen |  | Indexiert und durchsucht Live-Graph nach Substring |
| `FUNC-serialize-graph` | Serialize Graph |  | Serializes graph state to export format |
| `FUNC-subscribe-updates` | Subscribe Updates |  | Establishes WebSocket subscription for user |
| `FUNC-validate-mutation` | Validate Mutation |  | Validates incoming mutation against governance rules |
| `FUNC-validate-mutations` | Validate Mutations |  | Applies V3_RULES governance checks to batch |

## MS

| uid | name | status | description |
|---|---|---|---|
| `MS-1-auth-session` | M1: Auth & Session | open | Nutzer-Authentifizierung + Session-Erzeugung — Fundament für alle gated Operationen. |
| `MS-2-apply-gate-core` | M2: Apply-Gate Core | open | Validierung, Approval, Persistenz der Batch-Mutations — der Kern des Apply-Gate. |
| `MS-3-export-search` | M3: Export & Search | open | Graph-Serialisierung + Suchindex/-filter über den persistierten Graphen. |
| `MS-4-live-updates` | M4: Live Updates | open | WebSocket-Broadcast + Client-Subscription für Echtzeit-UI-Updates. |

## REQ

| uid | name | status | description |
|---|---|---|---|
| `REQ-apply-gate-verdict-deterministic` | Apply-Gate Verdicts sind deterministisch und reproduzierbar |  | Für identische Eingabe-Batches muss Apply-Gate identisches Verdict generieren. Messkriterium: 100% Verdict-Reproduzierbarkeit über 1000 Test-Runs; keine Nondeterminismus. |
| `REQ-author-gate-validation` | Pre-Gate Governance-Validierung |  | System muss Element-Validierung gegen Regeln vor Apply-Gate durchführen |
| `REQ-author-input-validation` | Elementeingabe validiert gegen Ontologie vor Submit |  | System muss Nutzer-Input (uid, type, name, description, attributes) gegen das Ontologie-Schema validieren BEVOR Batch ins Apply-Gate geht. Messkriterium: Alle ungültigen Inputs werden mit >95% Precision erkannt und UI zeigt Fehler <200ms nach keyup. |
| `REQ-author-integrity` | Node authored by collaborators passes apply-gate |  | Batch mutations from multiple users atomically applied |
| `REQ-batch-atomicity-measurable` | Batch-Mutation ist atomar (All-or-Nothing) mit messbarem Kriterium |  | Wenn ein Kommando im Batch gegen V3_RULES verstößt, wird der gesamte Batch abgelehnt (keine Partial Applies). Messkriterium: (1) graphVersion ändert sich nur bei tier=auto-apply oder tier=suggest mit Approval; bei tier=block bleibt graphVersion unverändert. (2) Für jeden fehlgeschlagenen Batch muss Apply-Gate Violations mit Regel-ID und fixHints zurückgeben. (3) Konsistenz-Test: 100 zufällige Batches mit 1 intentionalen Violation → 100% werden komplett abgelehnt. |
| `REQ-batch-validation-measurable` | Batch-Validierung gegen V3_RULES mit messbarem Kriterium |  | System muss Mutations-Batches gegen V3_RULES validieren und bei Violations korrekt blocken mit violations Array und fixHints. Messkriterium: (1) Für jeden Violation wird ein Eintrag mit ruleId, severity, message und fixHint generiert. (2) False-Negative-Rate <1% (maximal 1 unerkannte Violation pro 100 Test-Batches). (3) False-Positive-Rate <0.5% (maximal 0.5 fehlerhafte Ablehnungen pro 1000 gültigen Batches). |
| `REQ-concurrent-batch-isolation` | Concurrent Batches sind isoliert und deterministisch |  | Wenn mehrere Nutzer gleichzeitig Batches einreichen, muss das Apply-Gate diese sequenziell oder mit Optimistic Concurrency Control (OCC) verarbeiten. Messkriterium: (1) Final Graph-State ist deterministisch unabhängig von Submission-Reihenfolge (idempotent). (2) baseVersion-Check lehnt stale Batches (baseVersion < currentGraphVersion) ab. (3) Für 1000 concurrent-Batch-Szenarien: Keine Race Conditions, keine Inkonsistenzen. |
| `REQ-discover-latency-measurable` | Element-Discovery mit messbarem Latenz-Kriterium |  | Graph-Suchindex muss Element-Queries innerhalb von 200ms beantworten, ohne Mutations zu blockieren. Messkriterium: (1) P99-Latenz für Suche nach UID/Name <200ms bei bis zu 10.000 Elementen im Graph. (2) Während aktiver Mutations (Apply-Gate laufen) darf Search-Latenz nicht um mehr als 50% ansteigen. (3) Index wird innerhalb von 100ms nach erfolgreicher Mutation aktualisiert. |
| `REQ-e2e-collaboration-journey` | Kompletter Multiuser-Durchstich |  | Zwei Nutzer: Login → gemeinsames Autorieren durchs Apply-Gate → Live-Update beim jeweils anderen sichtbar → Export enthält beide Beiträge. Messbar: Durchstich < 60 s, Export bitweise reproduzierbar. |
| `REQ-export-snapshot-bitwise-identical` | Export-Snapshot ist bitwise-identisch mit in-memory Graph |  | Exportierter Graph ist bitwise-identisch mit dem in-memory Systemgraph. Messkriterium: Round-trip liefert identische Element-UIDs, Kanten und Attribute; SHA256-Hash ist deterministisch. |
| `REQ-export-versioning` | Versionierte Exporte mit Audit-Trail |  | Export muss Graphversion, Timestamp und Nutzer-Aktion enthalten |
| `REQ-governance-enforcement` | Governance-Enforcement im Apply-Gate |  | Requirement: Apply-Gate muss V3_RULES durchsetzen, ungültige Batches blocken (tier=block), Verdicts mit Regel-IDs und fixHints generieren, erfolgreiche Batches persistent machen. |
| `REQ-no-stale-data` | Keine veralteten Graph-Daten anzeigen |  | View enthält nur Daten, die nicht älter als die letzte Apply-Gate-Mutation sind |
| `REQ-search-filtering` | Suchfilterung nach Elementtyp |  | Requirement: System muss Elemente nach Typ (UC/REQ/TEST/etc) und Name filtern, Suchquery mit <100ms Latenz beantworten, keine False Positives. |
| `REQ-search-indexing` | Graphindex für Suche |  | Suchindex ermöglicht schnelle Abfragen über Live-Graph |
| `REQ-search-result-precision-recall` | Suchindex liefert vollständige und korrekte Treffer |  | System muss zu Query alle passenden Knoten zurückliefern (Recall 100%) und keine nicht-passenden (Precision 100%). Messkriterium: <200ms Latenz, exakte Substring-Matching (case-insensitive), Typ-Filter als logisches AND. |
| `REQ-session-token-created` | Session-Token wird erzeugt und zurückgegeben |  | Nach erfolgreicher Authentifizierung erhält Nutzer ein Session-Token zur Identifikation |
| `REQ-updates-visible-ui` | UI zeigt Graph-Änderungen Echtzeit |  | Mutations werden im UI-Baum sichtbar reflektiert, kein Manuell-Refresh nötig |
| `REQ-user-auth-email` | Nutzer authentifiziert mit Email und Passwort |  | Gültige Credentials authentifizieren den Nutzer gegenüber dem System |
| `REQ-user-discover-search-interface` | Suchoberfläche mit Filterung |  | System muss Nutzer-Suche über UID/Name mit Real-time-Filterung unterstützen |
| `REQ-view-current-state` | Nutzer sieht aktuellen Systemgraph-Zustand |  | Graph-View zeigt Live-Stand mit Knotendaten, Kanten und letztem Mutations-Timestamp |
| `REQ-websocket-live` | WebSocket-Verbindung überträgt Graph-Mutations live |  | Client empfängt Apply-Gate-Outputs als Events via WebSocket in <500ms |

## SYS

| uid | name | status | description |
|---|---|---|---|
| `SYS-multiuser-web-app` | Multiuser Web App |  | Multiuser-fähige Web-App aus dem graphcode harness: Nutzer melden sich an, autorieren gemeinsam einen governten Systemgraphen über das Apply-Gate, sehen Live-Updates und exportieren den Stand. |

## TEST

| uid | name | status | description |
|---|---|---|---|
| `TEST-auth-session` | Auth & Session-Lebenszyklus |  | Login mit Email/Passwort → Session-Token erzeugt und zurückgegeben; falsches Passwort → kein Token; Token-Ablauf erzwingt Re-Login. |
| `TEST-batch-validation` | Batch-Validierung verifizieren |  | Prüfschritt: Admin reicht Mutations-Batch ein, System validiert gegen V3_RULES, Violations werden korrekt identifiziert und geblockt |
| `TEST-e2e-multiuser-journey` | E2E Multiuser-Durchstich |  | Playwright, 2 Browser-Contexts: beide Nutzer anmelden, User A autoriert Element (Gate-applied), User B sieht Live-Update ohne Reload, Export-Download enthält As Element. Ein Lauf = ganze Pyramide-Spitze. |
| `TEST-export-roundtrip` | Export-Determinismus |  | Zweimal exportieren ohne Mutation → bitweise identisch; Export nach Mutation trägt neue Version; Re-Import (seed) reproduziert den Graphen verlustfrei. |
| `TEST-governance-enforcement` | Governance-Enforcement verifizieren |  | Prüfschritt: System enforced V3_RULES bei Apply-Gate, blockiert ungültige Mutations-Batches mit Verdicts, erlaubt nur compliant gates |
| `TEST-live-updates` | Live-Update-Strom |  | WebSocket-Subscription: persistierte Mutation erzeugt versioniertes Event; UI zeigt Änderung ohne Reload; Reconnect via Last-Event-ID liefert keinen veralteten Stand. |
| `TEST-search-filtering` | Suchfilterung verifizieren |  | Prüfschritt: Nutzer filterst Elemente nach Typ (UC/REQ/TEST), Ergebnisse werden korrekt gefiltert und angezeigt, Performance unter 100ms |
| `TEST-search-indexing` | Suchindex-Indexierung prüfen |  | Prüfschritt: (1) Systemstart triggert Index-Build über alle Graph-Elemente; (2) Live-Updates durch Apply-Gate sind sofort im Index sichtbar; (3) Suchquery über name/uid/description liefert <10ms auf 1000+ Knoten; (4) Filter-Kombinationen (type=REQ AND layer=arch) arbeiten korrekt |
| `TEST-verify-batch-validation` | Batch-Validierung vor Apply-Gate |  | Prüfschritt: (1) Admin submittet Batch mit absichtlichen Violations (z.B. orphaned TEST ohne testRef); (2) graph_mutate mit dryRun:true lehnt ab (success:false) mit violations und fixHints; (3) Admin korrigiert Batch; (4) Mit korrigiertem Batch dryRun:true geht durch (success:true); (5) nur der erfolgreiche Batch wird ohne dryRun persistent |
| `TEST-verify-governance-enforcement` | Governance-Regeln durchsetzen |  | Prüfschritt: (1) User2 submittet Batch mit UC ohne actor-Bindung (V3_RULES-Violation); (2) Gate-Verdict blockiert (tier=block) mit Regel-ID (z.B. R-5); (3) User2 sieht fixHints und fügt ACTOR io→UC ein; (4) Corrected Batch passiert Gate und wird applied; (5) Live-Graph zeigt neue UC+ACTOR sofort; (6) Report verzeichnet Regel und User+Timestamp |
| `TEST-verify-search-filtering` | Verify Suchfilterung |  | Prüfschritt: Nutzer gibt Filterkriterien ein (z.B. type:REQ, name:*batch*); System returned gefilterte Knotenliste; Verification: Alle Treffer entsprechen Filterkriterien, keine False Positives. |
| `TEST-verify-search-indexing` | Verify Graphindex |  | Prüfschritt: Index wird nach Batch-Mutation aktualisiert; Suche über Live-Graph liefert neue Elemente sofort; Verification: Indexierung synkt mit Graph-State, Query-Latenz <100ms. |

## UC

| uid | name | status | description |
|---|---|---|---|
| `UC-admin-approve-batch` | Admin autorisiert Batch |  | Admin reviewed und approveert Mutationsbatch durchs Apply-Gate |
| `UC-admin-enforce-governance` | Admin enforced Governance-Regeln |  | Admin durchsetzt V3_RULES und blockiert Violations |
| `UC-system-export-graph` | System exportiert Graphstand |  | Harness exportiert aktuellen Graphstand als Datei oder API |
| `UC-system-export-graph-versioned` | System exportiert versionierten Graphstand |  | Harness exportiert aktuellen Graph mit Versionsnummer und Audit-Log |
| `UC-user-author-node` | Nutzer erstellt Graphelement |  | Nutzer autorisiert neuen Knoten oder Kante im Systemgraph |
| `UC-user-author-node-with-validation` | Nutzer erstellt Element mit Governance-Validierung |  | Nutzer autorisiert Element; Harness validiert gegen V3_RULES vor Apply-Gate-Submission |
| `UC-user-discover-elements` | Nutzer sucht Elemente |  | Nutzer recherchiert Knoten nach UID oder Name im Graph |
| `UC-user-discover-elements-with-filter` | Nutzer sucht Elemente mit Filter |  | Nutzer recherchiert Knoten nach UID, Name oder Typ und filtert Live-Ergebnisse |
| `UC-user-login` | Nutzer meldet sich an |  | Nutzer authentifiziert und erhält Session zur Graphbearbeitung |
| `UC-user-monitor-graph-updates` | Monitor graph updates in real-time |  | User observes mutations from collaborators live via websocket |
| `UC-user-view-updates` | Nutzer beobachtet Live-Updates |  | Nutzer sieht Echtzeit-Änderungen anderer Aktoren im Graph |
