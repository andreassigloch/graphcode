<!-- GENERATED by @sigloch/graphcode exportMarkdown — DO NOT HAND-EDIT.
     Source of truth: docs/graph/graphcode.graph.json (the live graph).
     Re-render on model change: node scripts/export-graph.mjs -->

# graphcode — System Requirements Specification · SRS-graphcode

> GENERATED from `docs/graph/graphcode.graph.json` (SSOT). REQ-Slice nach ISO/IEC/IEEE 29148, gruppiert nach UC. 106 REQ. Deterministisch generiert.

## 1  Scope

`SYS-graphcode` — Headless Claude-Code-Sidecar-Governance-Harness: Store + Apply-Gate + Hooks + Learning-Emission, eine Instanz pro Repo. Carve-Out aus aimprove. (README, SPEC §0)

This SRS is the REQ-slice render of the live graph (SSOT). It is NOT the model spec (full dump).

## 2  References

@sigloch/contracts/se (ontology + V3_RULES) · ADR-001 (goal & constraints) · Format-E codec spec.

## 3  Definitions

Apply-Gate · mutate() · Trace pattern (TRACE_PATTERNS) · Readiness gate (SRR/PDR/CDR/TRR).

## 4  Requirements

### 4.1  `UC-code-quality` — Exzellente, governte Code-Qualität

`REQ-code-governed-quality`  priority: must · status: open · kinds: functional
  Statement      Jede Code-/Modell-Änderung ist gate-validiert (SE-Ontologie + V3_RULES), konsistent und driftfrei — kein ungeprüfter Schreibpfad.
  Verification   TEST-code-quality (), TEST-mvp-e2e (e2e)
  Trace          satisfy ◀ `FCHAIN-apply-gate` · allocate ▶ — · verify ◀ `TEST-code-quality` · `TEST-mvp-e2e`

`REQ-dashboard-ontology-sync`  priority: must · status: done · kinds: functional
  Statement      Das Dashboard/Readiness-Scorer MUSS gegen @sigloch/contracts Ontologie + V3_RULES evaluieren (via harness.evaluateRules, L2) — nicht die aimprove-Vorgänger-Regeln (rules 2.0.0, BQ-06/BQ-02 INCOSE). Heutige 155 BQ-Warnungen messen unsere REQs gegen eine Fremd-Regelbasis; nach Adoption echte Familie-Compliance. (NEXT REQ 2026-06-17)
  Verification   TEST-dashboard-ontology-sync (integration)
  Trace          satisfy ◀ `MOD-dashboard` · allocate ▶ — · verify ◀ `TEST-dashboard-ontology-sync`

`REQ-interface-change-escalation`  priority: must · status: open · kinds: functional
  Statement      Ein Realisierungs-Agent darf ein Interface (FLOW/SCHEMA) NICHT direkt mutieren. Bei Bedarf: (a) Notwendigkeit prüfen (sonst im Vertrag bleiben, Tech-Debt vermeiden); (b) CR an Facilitating-Agent + Boundary pausieren; (c) graph_impact(FLOW) Impact-Analyse; (d) Gate-Entscheidung (versionierte FLOW-Mutation / reject); (e) Dependents re-scopen/sequenzieren. Erhält conflict-free Parallelität; Interface-Drift zentral + gegatet.
  Verification   TEST-interface-escalation ()
  Trace          satisfy ◀ `FCHAIN-interface-escalation` · allocate ▶ — · verify ◀ `TEST-interface-escalation`

`REQ-quality-metric`  priority: should · status: open · kinds: non-functional
  Statement      Qualität = graph-eigene Metriken: (1) 0 error-Violations am Commit, (2) REQ→TEST-Traceability-Coverage, (3) keine Drift bei Re-Eval; gegen classic messbar. Definiert, was „exzellente Code-Qualität" (UC-code-quality) bedeutet — kein Vibe.
  Verification   TEST-code-quality ()
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-code-quality`

`REQ-structure-driven`  priority: should · status: open · kinds: non-functional
  Statement      Architektur/Interfaces/Integration/Tests werden strikt aus dem governten Graph abgeleitet (Schema-first), nicht ad-hoc.
  Verification   TEST-code-quality ()
  Trace          satisfy ◀ `SYS-graphcode` · allocate ▶ — · verify ◀ `TEST-code-quality`

### 4.2  `UC-efficient-testing` — Effizientes, impact-basiertes Testen

`REQ-impact-based-testing`  priority: must · status: open · kinds: functional
  Statement      Testset wird aus dem Impact-/Abhängigkeitsgraph bestimmt (richtige Tests statt alle); Coverage = nachgewiesen.
  Verification   TEST-efficient-testing (acceptance), TEST-mvp-e2e (e2e)
  Trace          satisfy ◀ `FCHAIN-impact-testing` · allocate ▶ — · verify ◀ `TEST-efficient-testing` · `TEST-mvp-e2e`

`REQ-test-runnable-binding`  priority: must · status: done · kinds: functional
  Statement      Jeder TEST-Knoten traegt einen testRef (Datei plus Case, tool, level), sodass ein impacted TEST-Knoten deterministisch zu einem lauffaehigen Artefakt aufgeloest werden kann. Familie-weit: testRef wird Teil des TEST-Element-Schemas in @sigloch/contracts/se (Minor-Version-Bump).
  Verification   TEST-test-runnable-binding (unit)
  Trace          satisfy ◀ `FUNC-deduce-tests` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-test-runnable-binding`

### 4.3  `UC-graph-time-travel` — Graph-Stand pro Commit wiederherstellbar

`REQ-graph-snapshot-per-commit`  priority: must · status: in-progress
  Statement      Jeder Commit traegt einen kanonischen, deterministischen Graph-Snapshot (docs/graph/*.graph.json), der zum Code dieses Commits passt. Un-exportierte Modell-Mutationen blockieren den Commit ueber den single-writer-sicheren Drift-Marker .graphcode/EXPORT_PENDING (vom Gate auf mutate gesetzt, von graph_export/graph_reseed geloescht); der pre-commit-Hook staged die generierten Artefakte automatisch. (CR-GC-217)
  Verification   TEST-graph-time-travel (integration)
  Trace          satisfy ◀ `FUNC-mutate` · allocate ▶ `MOD-harness` · verify ◀ `TEST-graph-time-travel`

`REQ-graph-state-recall`  priority: must · status: in-progress
  Statement      Ein frueherer Graph-Stand ist reproduzierbar via 'git checkout <sha>' + graph_reseed: der committete Snapshot ist SSOT-at-rest / history-of-record, der Kuzu-Store eine abgeleitete Working-Copy. reseed loescht+reimportiert den Store in-process hinter dem Single-Writer (kein zweites Handle). (CR-GC-217)
  Verification   TEST-graph-time-travel (integration)
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-graph-time-travel`

### 4.4  `UC-live-graph-view` — Live-Graph-View (Ziel b)

`REQ-artifact-freshness`  priority: must · status: done · kinds: functional
  Statement      Dokumente und Prozessschritte sind gruen wenn live aus dem aktuellen Graph abgeleitet, gelb wenn ein materialisiertes Doc existiert aber der Graph sich seither geaendert hat (stale), rot wenn noch nicht existent.
  Verification   TEST-artifact-freshness (integration)
  Trace          satisfy ◀ `FUNC-render-artifacts` · allocate ▶ `MOD-dashboard` · verify ◀ `TEST-artifact-freshness`

`REQ-dashboard-readonly`  priority: must · status: done · kinds: negative
  Statement      CONSTRAINT: Der Viewer ist strikt read-only. Kein Write-Pfad aus dem Browser (kein mutate/analyze/optimize/nightly-Trigger), kein Projekt-Switching (Single-Repo-Owner, CR-195e), Konsum nur ueber die Host-Bridge. Beleg: aimprove-Dashboard mischte Generator/Optimizer-Trigger ein; graphcode ist Harness-only.
  Verification   TEST-dashboard-readonly (integration)
  Trace          satisfy ◀ `MOD-dashboard` · allocate ▶ — · verify ◀ `TEST-dashboard-readonly`

`REQ-mutation-emits-event`  priority: should · status: done · kinds: non-functional
  Statement      CONSTRAINT: Jede Graph-Mutation MUSS ein Live-Update-Event emittieren (SSE invalidate, domains graph/rules/readiness/suggestions). Beleg: aimprove import emittiert keins → Dashboard-Lag ~90s. Fix: alle Write-Pfade einheitlich.
  Verification   TEST-live-view (integration)
  Trace          satisfy ◀ `FUNC-emit-update-event` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-live-view`

`REQ-readiness-transparent`  priority: must · status: done · kinds: functional
  Statement      Jedes Phase- und Impl-Gate exponiert seine Blocking-Elemente (welche CRs, Tests, UCs, Violations den Score druecken), sodass der Score auditierbar ist und keine Black-Box. In aimprove waren die Impl-Gates nie verdrahtet (0/9, 0/20, 0/4); graphcode macht die Herleitung transparent.
  Verification   TEST-readiness-transparent (integration)
  Trace          satisfy ◀ `FUNC-render-readiness` · allocate ▶ `MOD-dashboard` · verify ◀ `TEST-readiness-transparent`

`REQ-readonly-bridge`  priority: must · status: done · kinds: negative
  Statement      Bridge read-only; keine Inbound-Mutations, Writes nur via MCP→mutate(). (RECOMMENDATIONS)
  Verification   TEST-readonly-bridge (integration)
  Trace          satisfy ◀ `FUNC-serve-sse` · allocate ▶ `MOD-host-bridge` · verify ◀ `TEST-readonly-bridge`

`REQ-real-health-check`  priority: must · status: done · kinds: functional
  Statement      Der Health-Endpoint prueft Store-Erreichbarkeit, Gate-Funktion, Ontology/Rules/Contracts-Versionen und LLM/BYOK-Readiness, nicht nur Prozess-Liveness. aimprove-spezifische Felder (Sessions/Patterns) sind kein Health-Signal und werden entfernt.
  Verification   TEST-real-health-check (integration)
  Trace          satisfy ◀ `FUNC-health-endpoint` · allocate ▶ `MOD-host-bridge` · verify ◀ `TEST-real-health-check`

`REQ-versioned-broadcast`  priority: must · status: done · kinds: functional
  Statement      Versioned Diff-Broadcast an Dashboard (read-only, Late-Joiner-Cache). (R9)
  Verification   TEST-live-view (integration)
  Trace          satisfy ◀ `FUNC-broadcast-diff` · `FUNC-emit-update-event` · allocate ▶ `MOD-hooks` · `MOD-host-bridge` · verify ◀ `TEST-live-view`

### 4.5  `UC-reduced-llm` — Reduzierte LLM-Anforderungen

`REQ-small-model-viable`  priority: should · status: open · kinds: non-functional
  Statement      Deterministische, modellfreie Gates/Regeln + Query-Precision halten kleine/lokale LLMs tragfähig; Kern läuft ohne LLM (degraded).
  Verification   TEST-mvp-e2e (e2e), TEST-reduced-llm ()
  Trace          satisfy ◀ `FCHAIN-modelfree-gate` · allocate ▶ — · verify ◀ `TEST-mvp-e2e` · `TEST-reduced-llm`

### 4.6  `UC-token-efficiency` — Minimaler Token-Verbrauch

`REQ-benchmark-harness`  priority: must · status: open · kinds: functional
  Statement      Setting zum Vergleich graphcode-Modus vs. Claude-Code-classic über eine fixe Task-Suite, 2 LLMs (groß + klein/lokal), mit Token-Counter + Quality-Scorer. Liefert task×mode×LLM → {tokens, success, quality} und belegt token-efficiency + reduced-llm + code-quality. NUR Requirement — Harness-Bau ist Realisierung (eigene CR).
  Verification   TEST-token-efficiency ()
  Trace          satisfy ◀ `SYS-graphcode` · allocate ▶ — · verify ◀ `TEST-token-efficiency`

`REQ-precise-context`  priority: should · status: open · kinds: non-functional
  Statement      Kontext = exakter Blast-Radius/Sub-Graph-Slice (Format-E) statt grep-Dump/Result-Kompression.
  Verification   TEST-mvp-e2e (e2e), TEST-token-efficiency ()
  Trace          satisfy ◀ `FCHAIN-agent-query` · allocate ▶ — · verify ◀ `TEST-mvp-e2e` · `TEST-token-efficiency`

### 4.7  Cross-cutting / operational (no single UC)

`REQ-agent-agnostic`  priority: must · status: done
  Statement      Die MCP-stdio-Surface ist agent-agnostisch: Claude Code UND OpenCode (und jeder MCP-Client) treiben dieselbe Harness durchs selbe Gate; keine client-spezifischen Annahmen. (CLAUDE.md verriegelt: OpenCode-executed, Claude Code = ein Client)
  Verification   TEST-agent-agnostic (integration)
  Trace          satisfy ◀ `FUNC-serve-stdio` · `MOD-mcp-tools` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-agent-agnostic`

`REQ-audit-trail`  priority: must · status: open · kinds: functional
  Statement      CR-GC-101: audit_trail/audit_stats liefern Mutations-History/Statistik.
  Verification   TEST-mcp-stdio-server (integration)
  Trace          satisfy ◀ `FUNC-graph-impact` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-mcp-stdio-server`

`REQ-auto-persist-merge`  priority: must · status: open · kinds: functional
  Statement      Auto-Rebuild/Persist bei Commit + conflict-free Merge-Strategie fürs Graph-Artefakt. (R2)
  Verification   TEST-merge ()
  Trace          satisfy ◀ `FUNC-merge-nodes` · allocate ▶ `MOD-codec` · verify ◀ `TEST-merge`

`REQ-batch-seed-performance`  priority: must · status: done
  Statement      Seed/Import muss batch-skalieren: per-Row-MERGE ist O(langsam) (10k Edges ~51s, SP-2). UNWIND-Batch-Insert (gruppiert je Label/Edge-Table, Werte inline via escapeString) liefert 5k Nodes + 5k Edges < 15s (gemessen 5.3s, 5.6x schneller; Edges 9.6x). Interface unverändert (StorageAdapter.saveNodes/saveEdges), kein Parallelpfad. (CR-GC-120)
  Verification   TEST-batch-seed (performance)
  Trace          satisfy ◀ `FUNC-import` · allocate ▶ `MOD-harness` · verify ◀ `TEST-batch-seed`

`REQ-bootstrap-through-gate`  priority: must · status: done · kinds: functional
  Statement      FUNC-import: Erstbefüllung ausschließlich über das mutate()-Gate; Quelle = Format-E; kein Direct-Write.
  Verification   TEST-bootstrap (integration)
  Trace          satisfy ◀ `FUNC-import` · allocate ▶ `MOD-harness` · verify ◀ `TEST-bootstrap`

`REQ-buildable-standalone`  priority: should · status: done · kinds: non-functional
  Statement      CR-GC-100 Task 0 / SPEC §8 D5 (Blocker): workspace:*-Deps auflösen (versionierte/file-Deps), npm install + tsc --noEmit grün — vor jedem Code.
  Verification   TEST-distribution (e2e)
  Trace          satisfy ◀ `MOD-cli` · allocate ▶ — · verify ◀ `TEST-distribution`

`REQ-cache-layering`  priority: should · status: open · kinds: non-functional
  Statement      Nur Onto+Rules stabil cachen, nie mit Live-Graph; Prefix-Hygiene. (R8/R14)
  Verification   TEST-cache (integration)
  Trace          satisfy ◀ `FUNC-graph-expand` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-cache`

`REQ-codec-validation`  priority: must · status: open · kinds: functional
  Statement      CR-GC-103: encode/decode validiert gegen SE_DESCRIPTOR; ungültige Typen → Validierungsfehler, kein silent pass.
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FUNC-decode` · allocate ▶ `MOD-codec` · verify ◀ `TEST-roundtrip`

`REQ-confidence-tier`  priority: must · status: open · kinds: functional
  Statement      MutateResult trägt Confidence/Tier (auto-apply/suggest/block); speist 3-Tier-Gate. (R1)
  Verification   TEST-mutate-gate (integration)
  Trace          satisfy ◀ `FUNC-mutate` · allocate ▶ `MOD-harness` · verify ◀ `TEST-mutate-gate`

`REQ-conflict-free-merge`  priority: must · status: open · kinds: functional
  Statement      FUNC-merge-nodes: Branch-/Multi-Dev-Merge conflict-free (deterministische Serialisierung + merge_nodes).
  Verification   TEST-merge ()
  Trace          satisfy ◀ `FUNC-merge-nodes` · allocate ▶ `MOD-codec` · verify ◀ `TEST-merge`

`REQ-deterministic-serialization`  priority: should · status: open · kinds: non-functional
  Statement      Stabile Sortierung → commit-/merge-arm; zwei Encodes byte-identisch. (R3)
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FUNC-encode` · allocate ▶ `MOD-codec` · verify ◀ `TEST-roundtrip`

`REQ-disk-persistence`  priority: should · status: open · kinds: non-functional
  Statement      Persistenz auf Disk (.graphcode/kuzu/), kein :memory:. (SPEC §4)
  Verification   TEST-mvp-e2e (e2e)
  Trace          satisfy ◀ `FUNC-save-graph` · allocate ▶ `MOD-harness` · verify ◀ `TEST-mvp-e2e`

`REQ-doc-export`  priority: must · status: done · kinds: functional
  Statement      human-readable Docs (SPEC/Architektur/CR-Liste/References) werden DETERMINISTISCH aus dem Graph generiert (GENERATED-Header), nie hand-editiert. Schließt die graph-is-ssot-Drift-Lücke. (REQ-graph-is-ssot)
  Verification   TEST-doc-export (conformance), TEST-mcp-export (integration), TEST-member-name (unit), TEST-skills-mcp (integration)
  Trace          satisfy ◀ `FUNC-export-markdown` · `FUNC-render-views` · `FUNC-view-changelog` · `FUNC-view-conops` · `FUNC-view-icd` · `FUNC-view-intplan` · `FUNC-view-irr` · `FUNC-view-rtm` · allocate ▶ `MOD-dashboard` · `MOD-docs` · `MOD-skills` · verify ◀ `TEST-doc-export` · `TEST-mcp-export` · `TEST-member-name` · `TEST-skills-mcp`

`REQ-docs-taxonomy`  priority: should · status: reviewed · kinds: non-functional
  Statement      CONSTRAINT (non-functional): Aus dem Graphen reproduzierbar → VIEW (generiert, disposable, byte-deterministisch); friert datiertes Urteil/externen Input ein → RECORD (immutable Nachweis). Verfeinert REQ-graph-is-ssot. Ziel-Layout docs/{graph,views,records,adr,cr}. (CR-GC-119)
  Verification   TEST-docs-taxonomy ()
  Trace          satisfy ◀ `MOD-docs` · allocate ▶ — · verify ◀ `TEST-docs-taxonomy`

`REQ-export-no-clobber`  priority: should · status: done · kinds: non-functional
  Statement      graph_export verweigert, wenn es committete Elemente/Traces droppen wuerde: Empty-Guard (0 Elemente) + Net-Deletion-Guard (Diff gegen committetes JSON); force:true als explizites Opt-in fuer beabsichtigte Loeschungen. Sicherheitsnetz gegen stale/parallel-writer Clobber. (CR-GC-202)
  Verification   TEST-mcp-export-guard (integration)
  Trace          satisfy ◀ `MOD-mcp-tools` · allocate ▶ — · verify ◀ `TEST-mcp-export-guard`

`REQ-formatE-diff-dialect`  priority: must · status: open · kinds: functional
  Statement      CR-GC-103 R5: Diff-Dialekt +/-/~/M mit <operations><base_snapshot>ID@version + 1:N-Grouping; implicit-add VERWERFEN.
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FUNC-encode` · allocate ▶ `MOD-codec` · verify ◀ `TEST-roundtrip`

`REQ-formatE-parity`  priority: should · status: open · kinds: non-functional
  Statement      CR-GC-103 L1: Format-E-Parität = contracts-Baseline; genau EIN Codec.
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FUNC-encode` · allocate ▶ `MOD-codec` · verify ◀ `TEST-roundtrip`

`REQ-frame-binding`  priority: should · status: open · kinds: non-functional
  Statement      Beschluss 2026-06-16: Die in diesem Graph definierte Struktur + Interfaces (6 MOD, 4 Customer-UC, FUNC/FCHAIN/FLOW/REQ + SE-Ontologie/TRACE_PATTERNS) sind BINDEND für die Realisierung. Ergänzungen NUR, wenn sie in die vordefinierten Boxen passen (neue FUNC/FLOW/REQ/TEST an bestehendem MOD/UC durchs Gate). Strukturelle Änderungen — neues sigloch-modules-Shared, neuer ElementType/TraceType, neue Customer-UC/MOD — brauchen Familie-Review.
  Verification   TEST-code-quality ()
  Trace          satisfy ◀ `SYS-graphcode` · allocate ▶ — · verify ◀ `TEST-code-quality`

`REQ-gate-only-writes`  priority: should · status: open · kinds: non-functional
  Statement      Agent kann den SSOT nicht hand-editieren; jeder Write durch graph_mutate (mutate-Gate, L1). Deny-Rule + PreToolUse-Hook auf Edit/Write von docs/graph/*.graph.json + .graphcode/kuzu; JSON ist generierter Export, Live-Truth ist Kuzu. (CR-GC-201)
  Verification   TEST-no-direct-graph-write ()
  Trace          satisfy ◀ `FUNC-mutate` · allocate ▶ `MOD-harness` · verify ◀ `TEST-no-direct-graph-write`

`REQ-graceful-degradation`  priority: should · status: open · kinds: non-functional
  Statement      CONSTRAINT (ConOps): Harness voll funktionsfähig bei nicht erreichbarem LLM-Sidecar — Gate/Regeln deterministisch, kein Modell-Call.
  Verification   TEST-reduced-llm ()
  Trace          satisfy ◀ `FCHAIN-modelfree-gate` · `SYS-graphcode` · allocate ▶ — · verify ◀ `TEST-reduced-llm`

`REQ-graph-code-conformance`  priority: must · status: done
  Statement      Jeder FUNC.codeRef loest auf ein real deklariertes Symbol in seiner Datei auf (TypeScript-Parser, kein Substring-Match); prompt-realisierte FUNCs (lang prompt): die Skill-Datei existiert. Macht das R-20-Backfill verifizierbar statt nur vorhanden. (CR-GC-206)
  Verification   TEST-code-conformance (integration)
  Trace          satisfy ◀ `FUNC-check-code-conformance` · allocate ▶ — · verify ◀ `TEST-code-conformance`

`REQ-graph-integrity`  priority: should · status: done · kinds: non-functional
  Statement      Ein Validierungs-Pfad an EINEM Punkt (Gate): GraphCodeCodec.validate() erkennt zusaetzlich doppelte UIDs (nodeTypeMap dedupt heute still) + referenzielle Integritaet + valide Edge-Paare; kein inline validPairs-Klon ausserhalb des Codecs (keine parallelen Pfade). Das Apply-Gate ruft validate() VOR persist() auf (gleicher contracts-SSOT, Delta-Semantik), sodass kein strukturell-ungueltiges Edge den Store erreicht und Kuzu-DDL + Export-Check zu Backstops werden. (CR-GC-200)
  Verification   TEST-codec-validation (integration), TEST-graph-integrity (integration)
  Trace          satisfy ◀ `MOD-codec` · allocate ▶ — · verify ◀ `TEST-codec-validation` · `TEST-graph-integrity`

`REQ-graph-is-ssot`  priority: should · status: done · kinds: non-functional
  Statement      Der materialisierte Graph + die Live-Harness sind SSOT. docs/*.md sind historischer Input (Bootstrap). Modelländerungen am Graph (mutate/import), dann Re-Export. (2026-06-14)
  Verification   TEST-graph-is-ssot (integration)
  Trace          satisfy ◀ — · allocate ▶ — · verify ◀ `TEST-graph-is-ssot`

`REQ-graph-tests-operational`  priority: must · status: done
  Statement      Ein Code-Changeset (MOD/FUNC/git-diff→Knoten) → graph_tests → vitest run nur-betroffene-Dateien, das JEDE vom Change berührte Testdatei enthält (kein false-green) und unbeteiligte ausschliesst. Erfordert testRef auf allen lauffähigen TESTs + gerichtete Auflösung statt reinem incoming-impact.
  Verification   TEST-graph-tests-operational (integration)
  Trace          satisfy ◀ `FUNC-resolve-tests-from-code` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-graph-tests-operational`

`REQ-harness-schema-in-contracts`  priority: should · status: open · kinds: non-functional
  Statement      CR-GC-100 Task 1 / D1: HarnessConfig/MutateCommand/MutateResult nach @sigloch/contracts (eigener harness-Export, NICHT /se), importieren, lokale Defs löschen.
  Verification   TEST-mcp-symmetry (integration)
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-mcp-symmetry`

`REQ-hook-extension-points`  priority: must · status: open · kinds: functional
  Statement      CR-GC-102: registerHook(type, handler) + runPreCommitHooks/runPostApplyHooks/scheduleNightlyBatch; Storage .graphcode/hooks/.
  Verification   TEST-hooks (integration)
  Trace          satisfy ◀ `MOD-hooks` · allocate ▶ — · verify ◀ `TEST-hooks`

`REQ-hook-order-deterministic`  priority: should · status: open · kinds: non-functional
  Statement      CR-GC-102 L3: Hook-Execution-Order stabil/deterministisch.
  Verification   TEST-hooks (integration)
  Trace          satisfy ◀ `MOD-hooks` · allocate ▶ — · verify ◀ `TEST-hooks`

`REQ-import-se-ontology`  priority: should · status: open · kinds: non-functional
  Statement      SE-Ontologie aus @sigloch/contracts/se importieren, nicht lokal neu definieren. (SPEC §1, Drift D1)
  Verification   TEST-dashboard-ontology-sync (integration)
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-dashboard-ontology-sync`

`REQ-install-idempotent`  priority: should · status: open · kinds: non-functional
  Statement      Install/Update idempotent; alte Versionen überschrieben/gelöscht, nicht dupliziert.
  Verification   TEST-cli-scaffold (integration)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-cli-scaffold`

`REQ-interactive-capture-suggest`  priority: must · status: open · kinds: functional
  Statement      FCHAIN-capture: NL→Format-E agent-seitig (REQ-no-extraction); Resultat im suggest-Tier durchs Gate, Review vor Persist.
  Verification   TEST-capture ()
  Trace          satisfy ◀ `FCHAIN-capture` · allocate ▶ — · verify ◀ `TEST-capture`

`REQ-interface-schema`  priority: should · status: open · kinds: non-functional
  Statement      Jeder FLOW (Interface) hat ein SCHEMA (Layer 2, Datenformat) — referenziert @sigloch/contracts Zod. Code-Precondition: ohne Datenvertrag rät der Agent das Format. (3-Schichten-Interface-Modell)
  Verification   TEST-interface-schema ()
  Trace          satisfy ◀ `MOD-codec` · allocate ▶ — · verify ◀ `TEST-interface-schema`

`REQ-live-event-in-contracts`  priority: must · status: done
  Statement      LiveUpdateEvent/UpdateDomain (heute nur graphcode src/emit.ts) als Zod-Schema nach @sigloch/contracts publishen, damit Dashboard/Bridge denselben Vertrag importieren (kein Fork). Analog D1.
  Verification   TEST-live-event-contract (integration)
  Trace          satisfy ◀ `FUNC-emit-update-event` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-live-event-contract`

`REQ-mcp-gate-symmetry`  priority: should · status: done · kinds: non-functional
  Statement      CR-GC-101 L2: MCP graph_mutate == in-process mutate() — identische Semantik, identisches Violations-Dict (end-to-end).
  Verification   TEST-mcp-stdio-server (integration), TEST-mcp-symmetry (integration)
  Trace          satisfy ◀ `FCHAIN-apply-gate` · allocate ▶ — · verify ◀ `TEST-mcp-stdio-server` · `TEST-mcp-symmetry`

`REQ-mcp-tool-registry`  priority: must · status: done · kinds: functional
  Statement      CR-GC-101: Registry graph_elements/get_node/get_edges (read), graph_mutate (write durchs Gate), rules_evaluate/get_violations, audit_trail/stats — via bindToolsToHarness.
  Verification   TEST-mcp-readiness (integration), TEST-mcp-stdio-server (integration)
  Trace          satisfy ◀ `MOD-mcp-tools` · allocate ▶ — · verify ◀ `TEST-mcp-readiness` · `TEST-mcp-stdio-server`

`REQ-no-extraction`  priority: must · status: open · kinds: negative
  Statement      Keine tree-sitter/AST/LLM-Extraktion; Extraktion ist Slicer-/graphify-Aufgabe. (RECOMMENDATIONS)
  Verification   TEST-capture ()
  Trace          satisfy ◀ `FCHAIN-capture` · allocate ▶ — · verify ◀ `TEST-capture`

`REQ-npx-distribution`  priority: must · status: done · kinds: functional
  Statement      Distribution als npm-Paket mit bin `npx @sigloch/graphcode init/update/remove`. GATED auf REQ-buildable-standalone + CR-GC-100..103.
  Verification   TEST-distribution (e2e)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-distribution`

`REQ-one-gate-per-repo`  priority: must · status: open · kinds: functional
  Statement      Jede Edit-Op (Mensch oder KI) durch denselben mutate()-Pfad; consumerType nur geloggt. (SPEC §3, L1)
  Verification   TEST-mcp-symmetry (integration), TEST-mutate-gate (integration)
  Trace          satisfy ◀ — · allocate ▶ — · verify ◀ `TEST-mcp-symmetry` · `TEST-mutate-gate`

`REQ-post-agent-query`  priority: must · status: open · kinds: postcondition
  Statement      Exakter Subgraph als Format-E; on-demand vertieft; Graph read-only unverändert.
  Verification   TEST-impact-subgraph (integration)
  Trace          satisfy ◀ `FCHAIN-agent-query` · allocate ▶ — · verify ◀ `TEST-impact-subgraph`

`REQ-post-apply-gate`  priority: must · status: open · kinds: postcondition
  Statement      Gültige Ops persistiert oder bei error-Severity geblockt; consumerType geloggt; Live-Event + Trajectory emittiert.
  Verification   TEST-mutate-gate (integration)
  Trace          satisfy ◀ `FCHAIN-apply-gate` · allocate ▶ — · verify ◀ `TEST-mutate-gate`

`REQ-post-capture`  priority: must · status: open · kinds: postcondition
  Statement      Format-E-Kandidaten im suggest-Tier durchs Gate; kein auto-apply; Review vor Persist.
  Verification   TEST-capture ()
  Trace          satisfy ◀ `FCHAIN-capture` · allocate ▶ — · verify ◀ `TEST-capture`

`REQ-post-codec-roundtrip`  priority: must · status: open · kinds: postcondition
  Statement      decode(encode(g))==g; byte-identisch; ungültige Typen → Validierungsfehler.
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FCHAIN-codec-roundtrip` · allocate ▶ — · verify ◀ `TEST-roundtrip`

`REQ-post-emit-trajectory`  priority: must · status: open · kinds: postcondition
  Statement      Trajectory/Outcome append-only nach .aimprove/*.jsonl, Format stabil.
  Verification   TEST-learning-emit (integration)
  Trace          satisfy ◀ `FUNC-emit-trajectory` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-learning-emit`

`REQ-post-emit-update-event`  priority: must · status: open · kinds: postcondition
  Statement      Genau ein Live-Update-Event mit korrekten domains; ausschließlich read-only.
  Verification   TEST-live-view (integration)
  Trace          satisfy ◀ `FUNC-emit-update-event` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-live-view`

`REQ-post-export-markdown`  priority: must · status: done · kinds: postcondition
  Statement      Deterministische Markdown-Views mit GENERATED-Header; nie hand-editiert.
  Verification   TEST-doc-export (conformance)
  Trace          satisfy ◀ `FUNC-export-markdown` · allocate ▶ `MOD-docs` · verify ◀ `TEST-doc-export`

`REQ-post-harness-cli`  priority: must · status: done · kinds: postcondition
  Statement      Artefakte installiert/aktualisiert/restlos entfernt; idempotent; Store bei Update erhalten.
  Verification   TEST-cli-scaffold (integration)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-cli-scaffold`

`REQ-post-impact-testing`  priority: must · status: open · kinds: postcondition
  Statement      Genau die betroffenen Tests ausgewählt (bottom-up); nicht betroffene ausgelassen.
  Verification   TEST-efficient-testing (acceptance)
  Trace          satisfy ◀ `FCHAIN-impact-testing` · allocate ▶ — · verify ◀ `TEST-efficient-testing`

`REQ-post-import`  priority: must · status: done · kinds: postcondition
  Statement      Graph durchs Gate befüllt; kein Direct-Write; Violations berichtet.
  Verification   TEST-bootstrap (integration)
  Trace          satisfy ◀ `FUNC-import` · allocate ▶ `MOD-harness` · verify ◀ `TEST-bootstrap`

`REQ-post-interface-escalation`  priority: must · status: open · kinds: postcondition
  Statement      Interface NICHT direkt mutiert; eskaliert, impact-analysiert, gegatet, Dependents sequenziert — oder verworfen (im Vertrag bleiben).
  Verification   TEST-interface-escalation ()
  Trace          satisfy ◀ `FCHAIN-interface-escalation` · allocate ▶ — · verify ◀ `TEST-interface-escalation`

`REQ-post-merge-nodes`  priority: must · status: open · kinds: postcondition
  Statement      Conflict-free gemerged; keine verlorenen Knoten/Traces; deterministisch sortiert.
  Verification   TEST-merge ()
  Trace          satisfy ◀ `FUNC-merge-nodes` · allocate ▶ `MOD-codec` · verify ◀ `TEST-merge`

`REQ-post-migrate-schema`  priority: must · status: open · kinds: postcondition
  Statement      Graph re-validiert/migriert; Violations berichtet; Artefakt-Version aktualisiert.
  Verification   TEST-schema-migration ()
  Trace          satisfy ◀ `FUNC-migrate-schema` · allocate ▶ `MOD-harness` · verify ◀ `TEST-schema-migration`

`REQ-post-modelfree-gate`  priority: must · status: open · kinds: postcondition
  Statement      Apply + Regelprüfung deterministisch, kein Modell-Call; nur LLM-Zusatzfeatures degradieren.
  Verification   TEST-reduced-llm ()
  Trace          satisfy ◀ `FCHAIN-modelfree-gate` · allocate ▶ — · verify ◀ `TEST-reduced-llm`

`REQ-pre-agent-query`  priority: must · status: open · kinds: precondition
  Statement      Graph geladen, MCP-Tools gebunden, elementId existiert.
  Verification   TEST-impact-subgraph (integration)
  Trace          satisfy ◀ `FCHAIN-agent-query` · allocate ▶ — · verify ◀ `TEST-impact-subgraph`

`REQ-pre-apply-gate`  priority: must · status: open · kinds: precondition
  Statement      Harness initialisiert, Graph geladen, gültige MutateCommands liegen vor.
  Verification   TEST-mutate-gate (integration)
  Trace          satisfy ◀ `FCHAIN-apply-gate` · allocate ▶ — · verify ◀ `TEST-mutate-gate`

`REQ-pre-capture`  priority: must · status: open · kinds: precondition
  Statement      NL/Text-Eingang (chat-tauglich) + Agent verfügbar.
  Verification   TEST-capture ()
  Trace          satisfy ◀ `FCHAIN-capture` · allocate ▶ — · verify ◀ `TEST-capture`

`REQ-pre-codec-roundtrip`  priority: must · status: open · kinds: precondition
  Statement      Gültiger OntologyGraph (encode) bzw. Format-E-JSON (decode).
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FCHAIN-codec-roundtrip` · allocate ▶ — · verify ◀ `TEST-roundtrip`

`REQ-pre-emit-trajectory`  priority: must · status: open · kinds: precondition
  Statement      Mutation erfolgreich applied (post-apply-Phase).
  Verification   TEST-learning-emit (integration)
  Trace          satisfy ◀ `FUNC-emit-trajectory` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-learning-emit`

`REQ-pre-emit-update-event`  priority: must · status: open · kinds: precondition
  Statement      Mutation committed; SSE/WS-Bridge verbunden.
  Verification   TEST-live-view (integration)
  Trace          satisfy ◀ `FUNC-emit-update-event` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-live-view`

`REQ-pre-export-markdown`  priority: must · status: done · kinds: precondition
  Statement      Aktueller Graph (SSOT) geladen; View gewählt.
  Verification   TEST-doc-export (conformance)
  Trace          satisfy ◀ `FUNC-export-markdown` · allocate ▶ `MOD-docs` · verify ◀ `TEST-doc-export`

`REQ-pre-harness-cli`  priority: must · status: done · kinds: precondition
  Statement      Repo vorhanden, npx/Node verfügbar.
  Verification   TEST-cli-scaffold (integration)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-cli-scaffold`

`REQ-pre-impact-testing`  priority: must · status: open · kinds: precondition
  Statement      Geändertes Element bekannt; Graph geladen.
  Verification   TEST-efficient-testing (acceptance)
  Trace          satisfy ◀ `FCHAIN-impact-testing` · allocate ▶ — · verify ◀ `TEST-efficient-testing`

`REQ-pre-import`  priority: must · status: done · kinds: precondition
  Statement      Leerer/zu befüllender Repo-Graph; Format-E-Quelle (graphify/Import) liegt vor.
  Verification   TEST-bootstrap (integration)
  Trace          satisfy ◀ `FUNC-import` · allocate ▶ `MOD-harness` · verify ◀ `TEST-bootstrap`

`REQ-pre-interface-escalation`  priority: must · status: open · kinds: precondition
  Statement      Realisierungs-Agent stellt fest: bestehender FLOW-Vertrag reicht nicht.
  Verification   TEST-interface-escalation ()
  Trace          satisfy ◀ `FCHAIN-interface-escalation` · allocate ▶ — · verify ◀ `TEST-interface-escalation`

`REQ-pre-merge-nodes`  priority: must · status: open · kinds: precondition
  Statement      Zwei (oder mehr) Branch-Versionen des committed Graph-Artefakts.
  Verification   TEST-merge ()
  Trace          satisfy ◀ `FUNC-merge-nodes` · allocate ▶ `MOD-codec` · verify ◀ `TEST-merge`

`REQ-pre-migrate-schema`  priority: must · status: open · kinds: precondition
  Statement      Version-Bump in contracts/se; bestehender Graph auf alter Version.
  Verification   TEST-schema-migration ()
  Trace          satisfy ◀ `FUNC-migrate-schema` · allocate ▶ `MOD-harness` · verify ◀ `TEST-schema-migration`

`REQ-pre-modelfree-gate`  priority: must · status: open · kinds: precondition
  Statement      MutateCommands liegen vor; LLM-Sidecar evtl. nicht erreichbar.
  Verification   TEST-reduced-llm ()
  Trace          satisfy ◀ `FCHAIN-modelfree-gate` · allocate ▶ — · verify ◀ `TEST-reduced-llm`

`REQ-precommit-timeout`  priority: should · status: open · kinds: non-functional
  Statement      CR-GC-102: pre-commit-Hook kann eine Mutation blocken; preCommitTimeout (default 5000ms).
  Verification   TEST-hooks (integration)
  Trace          satisfy ◀ `MOD-hooks` · allocate ▶ — · verify ◀ `TEST-hooks`

`REQ-progressive-expansion`  priority: must · status: done · kinds: functional
  Statement      graph_expand vertieft on-demand (Kuzu-Re-Traversierung, kein Originals-Store). (R13)
  Verification   TEST-impact-subgraph (integration)
  Trace          satisfy ◀ `FUNC-graph-expand` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-impact-subgraph`

`REQ-query-precision`  priority: must · status: done · kinds: functional
  Statement      graph_impact liefert exakten Blast-Radius als Format-E (Anti-grep, Ziel a). (R6/R12)
  Verification   TEST-impact-subgraph (integration)
  Trace          satisfy ◀ `FUNC-graph-impact` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-impact-subgraph`

`REQ-readiness-model`  priority: must · status: done · kinds: functional
  Statement      Readiness-Modell fuer graphcode, definiert gegen @sigloch/contracts V3_RULES + die MS-Meilensteine + Element-Status (keine aimprove-BQ-Heuristik). INCOSE-Scope LEAN: der gegatete Graph ist das einzige SE-Artefakt. Phase-Readiness SRR/PDR/CDR/TRR ist eine disjunkte, vollstaendige Partition der 15 Element-Regeln; Implementation-Readiness SAR/FCA/SVR/FRR bindet die Meilenstein-Tiers MS-1..4 (ready wenn zugeordnete CRs done + Scope fehlerfrei) und deckt die 2 MS-Regeln ab. Realisiert im Scorer src/readiness.ts, exponiert ueber graph_readiness. (CR-GC-125)
  Verification   TEST-readiness-model (acceptance)
  Trace          satisfy ◀ `MOD-mcp-tools` · allocate ▶ — · verify ◀ `TEST-readiness-model`

`REQ-repo-install`  priority: must · status: done · kinds: functional
  Statement      Installation der Harness in ein beliebiges Repo mit einem Kommando: scaffolds .graphcode/, .claude/hooks, .mcp.json, Controller.
  Verification   TEST-cli-scaffold (integration), TEST-distribution (e2e), TEST-scaffold-skills (integration)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-cli-scaffold` · `TEST-distribution` · `TEST-scaffold-skills`

`REQ-repo-uninstall`  priority: must · status: open · kinds: functional
  Statement      Deinstallation entfernt alle installierten Artefakte ohne Residuen.
  Verification   TEST-cli-scaffold (integration)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-cli-scaffold`

`REQ-repo-update`  priority: must · status: open · kinds: functional
  Statement      Update aktualisiert installierte Artefakte/Pfade, ohne den lokalen Graph-Store (.graphcode/) zu verlieren.
  Verification   TEST-cli-scaffold (integration)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-cli-scaffold`

`REQ-responsiveness`  priority: should · status: open · kinds: non-functional
  Statement      Bindende NFR (Familie §6b): erste Reaktion < 0,2s (UI+Transport+Store-Query+Onto-/Rule-Check, ohne LLM). Draft-Apply sofort + nur betroffener Subgraph geprüft; volle Konsistenz am Commit. End-to-end über FCHAIN-apply-gate.
  Verification   TEST-responsiveness ()
  Trace          satisfy ◀ `FCHAIN-apply-gate` · allocate ▶ — · verify ◀ `TEST-responsiveness`

`REQ-roundtrip-conformance`  priority: should · status: open · kinds: non-functional
  Statement      decode(encode(g)) == g modulo Whitespace (rasentraktor-Fixture). (L3)
  Verification   TEST-roundtrip (conformance)
  Trace          satisfy ◀ `FCHAIN-codec-roundtrip` · `FUNC-decode` · allocate ▶ `MOD-codec` · verify ◀ `TEST-roundtrip`

`REQ-rule-enforcement`  priority: must · status: done · kinds: functional
  Statement      evaluateRules() gegen V3_RULES; error-Severity blockt den Apply. (L2)
  Verification   TEST-mutate-gate (integration)
  Trace          satisfy ◀ `FUNC-evaluate-rules` · allocate ▶ `MOD-harness` · verify ◀ `TEST-mutate-gate`

`REQ-schema-version-migration`  priority: must · status: open · kinds: functional
  Statement      FUNC-migrate-schema: bei Version-Bump re-validieren/migrieren, Violations berichten, Version mitführen.
  Verification   TEST-schema-migration ()
  Trace          satisfy ◀ `FUNC-migrate-schema` · allocate ▶ `MOD-harness` · verify ◀ `TEST-schema-migration`

`REQ-self-contained-dist`  priority: should · status: done · kinds: non-functional
  Statement      Zielprojekt darf NICHT von einer Kopie des aimprove-Quellbaums abhängen; Distribution self-contained (versionierte Deps). Blockiert auf D5 + CR-GC-100..103.
  Verification   TEST-distribution (e2e)
  Trace          satisfy ◀ `FUNC-harness-cli` · allocate ▶ `MOD-cli` · verify ◀ `TEST-distribution`

`REQ-shared-views-no-fork`  priority: must · status: done
  Statement      views.ts (testmatrix/FMEA/RTM/IRR/NFR/arch…) aus aimprove → @sigloch/graph-api-core; lokalen BQ-Regel-Fork (aimpro/src/contracts/se) retiren oder via Familie-Review nach contracts migrieren (Drift-Lock L1/L2).
  Verification   TEST-shared-views-no-fork (integration)
  Trace          satisfy ◀ `MOD-dashboard` · allocate ▶ — · verify ◀ `TEST-shared-views-no-fork`

`REQ-single-kuzu-owner`  priority: should · status: done · kinds: non-functional
  Statement      Genau ein Host-Prozess besitzt .graphcode/kuzu (single-writer; kein 2. DB-Handle). (SPEC §4, L1)
  Verification   TEST-mvp-e2e (e2e)
  Trace          satisfy ◀ `FUNC-own-kuzu-host` · `MOD-harness` · allocate ▶ `MOD-host-bridge` · verify ◀ `TEST-mvp-e2e`

`REQ-single-store`  priority: should · status: open · kinds: non-functional
  Statement      Genau ein Store = Kuzu (embedded native+WASM, Cypher). Kein Neo4j. (SPEC §0)
  Verification   TEST-mvp-e2e (e2e)
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-mvp-e2e`

`REQ-single-transport`  priority: should · status: done · kinds: non-functional
  Statement      Genau ein Transport = MCP-stdio; kein Express-REST/HTTP im Harness-Core. (SPEC §0, §5)
  Verification   TEST-mcp-stdio-server (integration)
  Trace          satisfy ◀ `FUNC-serve-stdio` · `MOD-mcp-tools` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-mcp-stdio-server`

`REQ-store-recovery`  priority: should · status: open · kinds: non-functional
  Statement      CONSTRAINT (ConOps): Recovery bei Kuzu Lock-Konflikt / abgestürztem Owner / korruptem Store — Lock-Erkennung + sicherer Re-Open.
  Verification   TEST-store-recovery (integration)
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-store-recovery`

`REQ-structural-rule-shared`  priority: must · status: done
  Statement      Trace-pair legality is enforced by the one shared engine rule R-18 (@sigloch/contracts/se), not a consumer-local validator. graphcodes Gate lehnt strukturell-invalide Mutationen via Engine ab (kein separater codec.validate-Aufruf); jeder Konsument der die Engine laeuft erbt die Pruefung. (CR-GC-205 Item 1)
  Verification   TEST-structural-rule-shared (integration)
  Trace          satisfy ◀ `MOD-harness` · allocate ▶ — · verify ◀ `TEST-structural-rule-shared`

`REQ-subgraph-slicing`  priority: must · status: open · kinds: functional
  Statement      Sub-Graph-Slicing + pruneToFit(maxTokens) als Context-Primitive. (R7)
  Verification   TEST-impact-subgraph (integration)
  Trace          satisfy ◀ `FUNC-graph-impact` · allocate ▶ `MOD-mcp-tools` · verify ◀ `TEST-impact-subgraph`

`REQ-testref-materialized`  priority: must · status: done
  Statement      Ein gebundener TEST-testRef loest immer auf eine reale Datei auf: graph_export materialisiert einen it.todo-Stub fuer jede fehlende testRef-Datei (kein Phantom-Pfad, kein false-green); R-19 macht einen ungebundenen lauffaehigen TEST als Warning sichtbar; concept-only TESTs (concept:true) sind ausgenommen. (CR-GC-205 Item 4)
  Verification   TEST-testref-materialize (integration)
  Trace          satisfy ◀ `MOD-docs` · `MOD-mcp-tools` · allocate ▶ — · verify ◀ `TEST-testref-materialize`

`REQ-trajectory-emit`  priority: must · status: open · kinds: functional
  Statement      post-apply/nightly: Trajectory/Outcome append-only nach .aimprove/*.jsonl, Format stabil. (CR-GC-102, L1)
  Verification   TEST-learning-emit (integration)
  Trace          satisfy ◀ `FUNC-emit-trajectory` · allocate ▶ `MOD-hooks` · verify ◀ `TEST-learning-emit`

`REQ-versioned-cache`  priority: should · status: open · kinds: non-functional
  Statement      CR-GC-102 R11: version-keyed Response-Cache + Dirty-Flag → auf Kuzu-Version mappen.
  Verification   TEST-cache (integration)
  Trace          satisfy ◀ `MOD-hooks` · allocate ▶ — · verify ◀ `TEST-cache`

## 5  Traceability summary

106 REQ · 106 verified · 0 without a verifying TEST (R-01).
